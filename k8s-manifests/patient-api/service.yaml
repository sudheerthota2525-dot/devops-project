apiVersion: v1
kind: Service
metadata:
  name: patient-api-service
  namespace: healthcare
spec:
  selector:
    app: patient-api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30081
  type: NodePort
